#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int singles;

	for (singles = 0; singles <= 9; singles++)
	{
		printf(singles);
	}
	putchar('\n');
	return (0);
}
