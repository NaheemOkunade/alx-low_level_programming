#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */
int main (void)
{
	char singles;
	for (singles = 0; single <= 9; singles++)
	{
		putchar(singles);
	}
	putchar('\n');
	return (0);
}
